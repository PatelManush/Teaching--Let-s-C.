# include <stdio.h>
int main(void)
{
	int a=23,b=5;

	if(a == b)
	{
		printf("Both numbers are equal\n");
	}
	else
	{
		printf("Numbers are not equal\n");
	}
        if(a <= b)
	{
	    printf("%d is greater than %d",a,b);
	}
	else
	{
	    printf("%d is greater than than %d",b,a);
	}

	return(0);
}
